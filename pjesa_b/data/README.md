# Data Folder

Place the official Electronic Online Shop dataset in this folder with this exact file name:

```text
electronic_online_shop.csv
```

The dataset must include the target column:

```text
DemandLevel
```

If the file is missing, the project code creates a generated sample dataset only so the notebook can demonstrate the workflow. Do not use the generated sample dataset as official project evidence unless your instructor explicitly allows synthetic demonstration data.
